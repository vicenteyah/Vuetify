export default class Feedback {
    constructor(completename,email,phone,store,message){
       this.completename = completename
       this.email = email
       this.phone = phone
       this.store = store
       this.message = message
    }
}